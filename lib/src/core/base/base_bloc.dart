

// import 'package:dartz/dartz.dart';
// import 'package:flutter_bloc/flutter_bloc.dart';

// abstract class BaseCubit<State> extends Cubit<State>{ 
//   BaseCubit(State initialState): super(initialState);

//   /// [R] is the response type data expected from the api call 
//   /// [AppError] is the error type returned from the server 
//   /// [onSuccess] and [onFailure] are callbacks that returns the
//   /// state of cubit and emits either success or failure states.

// Future<void>handleBusinessLogic<R>( 
//   { 
//     required Future<Either<AppError,R>> call, 
//     required 
//   }
// )
// }